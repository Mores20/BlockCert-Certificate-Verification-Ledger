# BlockCert-Certificate-Verification-Ledger
BlockCert is a blockchain-inspired web application that records certificate information as immutable transactions. Each certificate issued by a user is stored with a unique transaction ID (generated using SHA-256), allowing the integrity of certificate records to be verified.
